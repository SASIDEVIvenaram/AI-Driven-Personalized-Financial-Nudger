package com.team021.financial_nudger.config;

// Seeding disabled: users must add their own categories.
// Keeping an empty class to avoid runtime wiring but prevent warnings.
public class CategoryInitializer {
}